
weChatApp.controller('chatting-ctrl', ['$scope', '$timeout', "$stateParams", "$location",  function ($scope, $timeout, $stateParams, $location) {
    var myScroll = new IScroll('#chatting-wrapper', {
        scrollbars: true,
        bounce: false
    });
}]);