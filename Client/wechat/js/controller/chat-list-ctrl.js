
weChatApp.controller('chat-list-ctrl', ['$scope', '$timeout', "$stateParams", "$location",  function ($scope, $timeout, $stateParams, $location) {
    var myScroll = new IScroll('#chat-list-wrapper', {
        scrollbars: true,
        bounce: false
    });
}]);